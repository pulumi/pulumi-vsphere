// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * The `vsphere.SsoGroup` resource can be used to create and manage groups in the
 * vCenter Single Sign-On local (system) domain, including the users and nested
 * groups that belong to the group.
 *
 * > **NOTE:** Groups are always created in the local (system) domain. Group
 * membership, however, may include users from any identity source (see
 * `memberUser`).
 *
 * > **NOTE:** Membership is authoritative. The principals listed in
 * `memberUser` and `memberGroup` are the complete set of members.
 *
 * > **NOTE:** The connecting user must hold vCenter Single Sign-On administrator
 * privileges.
 *
 * ## Example Usage
 *
 * This example creates a local user and a group that contains that local user, a
 * user from an external identity source, and a nested local group.
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vsphere from "@pulumi/vsphere";
 *
 * const example = new vsphere.SsoUser("example", {
 *     name: "local.user",
 *     password: "P@ssw0rd123!",
 * });
 * const nested = new vsphere.SsoGroup("nested", {name: "engineering-leads"});
 * const exampleSsoGroup = new vsphere.SsoGroup("example", {
 *     name: "engineering",
 *     description: "Managed by Pulumi",
 *     memberUsers: [
 *         {
 *             name: example.name,
 *             domain: example.domain,
 *         },
 *         {
 *             name: "john.doe",
 *             domain: "example.com",
 *         },
 *     ],
 *     memberGroups: [{
 *         name: nested.name,
 *         domain: nested.domain,
 *     }],
 * });
 * ```
 *
 * ## Import
 *
 * An existing group can be imported into this resource by supplying its
 * `name@domain` identifier. An example is below:
 *
 * ```sh
 * $ pulumi import vsphere:index/ssoGroup:SsoGroup example engineering@vsphere.local
 * ```
 */
export class SsoGroup extends pulumi.CustomResource {
    /**
     * Get an existing SsoGroup resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: SsoGroupState, opts?: pulumi.CustomResourceOptions): SsoGroup {
        return new SsoGroup(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vsphere:index/ssoGroup:SsoGroup';

    /**
     * Returns true if the given object is an instance of SsoGroup.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is SsoGroup {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === SsoGroup.__pulumiType;
    }

    /**
     * A description of the group.
     */
    declare public readonly description: pulumi.Output<string | undefined>;
    /**
     * The identity source domain the group belongs to (the local/system
     * domain).
     */
    declare public /*out*/ readonly domain: pulumi.Output<string>;
    /**
     * The set of groups that are nested members of this
     * group. Members may come from any identity source. Each `memberGroup` block
     */
    declare public readonly memberGroups: pulumi.Output<outputs.SsoGroupMemberGroup[] | undefined>;
    /**
     * The set of users that are members of this group.
     * Members may come from any identity source. Each `memberUser` block supports
     * the following:
     */
    declare public readonly memberUsers: pulumi.Output<outputs.SsoGroupMemberUser[] | undefined>;
    /**
     * The name of the group. Forces a new resource if changed.
     */
    declare public readonly name: pulumi.Output<string>;

    /**
     * Create a SsoGroup resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: SsoGroupArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: SsoGroupArgs | SsoGroupState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as SsoGroupState | undefined;
            resourceInputs["description"] = state?.description;
            resourceInputs["domain"] = state?.domain;
            resourceInputs["memberGroups"] = state?.memberGroups;
            resourceInputs["memberUsers"] = state?.memberUsers;
            resourceInputs["name"] = state?.name;
        } else {
            const args = argsOrState as SsoGroupArgs | undefined;
            resourceInputs["description"] = args?.description;
            resourceInputs["memberGroups"] = args?.memberGroups;
            resourceInputs["memberUsers"] = args?.memberUsers;
            resourceInputs["name"] = args?.name;
            resourceInputs["domain"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(SsoGroup.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering SsoGroup resources.
 */
export interface SsoGroupState {
    /**
     * A description of the group.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The identity source domain the group belongs to (the local/system
     * domain).
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * The set of groups that are nested members of this
     * group. Members may come from any identity source. Each `memberGroup` block
     */
    memberGroups?: pulumi.Input<pulumi.Input<inputs.SsoGroupMemberGroup>[] | undefined>;
    /**
     * The set of users that are members of this group.
     * Members may come from any identity source. Each `memberUser` block supports
     * the following:
     */
    memberUsers?: pulumi.Input<pulumi.Input<inputs.SsoGroupMemberUser>[] | undefined>;
    /**
     * The name of the group. Forces a new resource if changed.
     */
    name?: pulumi.Input<string | undefined>;
}

/**
 * The set of arguments for constructing a SsoGroup resource.
 */
export interface SsoGroupArgs {
    /**
     * A description of the group.
     */
    description?: pulumi.Input<string | undefined>;
    /**
     * The set of groups that are nested members of this
     * group. Members may come from any identity source. Each `memberGroup` block
     */
    memberGroups?: pulumi.Input<pulumi.Input<inputs.SsoGroupMemberGroup>[] | undefined>;
    /**
     * The set of users that are members of this group.
     * Members may come from any identity source. Each `memberUser` block supports
     * the following:
     */
    memberUsers?: pulumi.Input<pulumi.Input<inputs.SsoGroupMemberUser>[] | undefined>;
    /**
     * The name of the group. Forces a new resource if changed.
     */
    name?: pulumi.Input<string | undefined>;
}
