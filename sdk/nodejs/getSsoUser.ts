// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * The `vsphere.SsoUser` data source can be used to look up a vCenter Single
 * Sign-On user by its username and domain, including users from external identity
 * sources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vsphere from "@pulumi/vsphere";
 *
 * // A user in the local (system) domain.
 * const local = vsphere.getSsoUser({
 *     name: "local.user",
 * });
 * // A user in an external identity source.
 * const external = vsphere.getSsoUser({
 *     name: "john.doe",
 *     domain: "example.com",
 * });
 * ```
 */
export function getSsoUser(args: GetSsoUserArgs, opts?: pulumi.InvokeOptions): Promise<GetSsoUserResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invoke("vsphere:index/getSsoUser:getSsoUser", {
        "domain": args.domain,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getSsoUser.
 */
export interface GetSsoUserArgs {
    /**
     * The identity source domain the user belongs to. Defaults
     * to the local (system) domain.
     */
    domain?: string;
    /**
     * The username of the user to look up.
     */
    name: string;
}

/**
 * A collection of values returned by getSsoUser.
 */
export interface GetSsoUserResult {
    /**
     * The description of the user.
     */
    readonly description: string;
    /**
     * The identity source domain the user belongs to.
     */
    readonly domain: string;
    /**
     * The email address of the user.
     */
    readonly emailAddress: string;
    /**
     * The first name of the user.
     */
    readonly firstName: string;
    /**
     * The provider-assigned unique ID for this managed resource.
     */
    readonly id: string;
    /**
     * The last name of the user.
     */
    readonly lastName: string;
    readonly name: string;
}
/**
 * The `vsphere.SsoUser` data source can be used to look up a vCenter Single
 * Sign-On user by its username and domain, including users from external identity
 * sources.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vsphere from "@pulumi/vsphere";
 *
 * // A user in the local (system) domain.
 * const local = vsphere.getSsoUser({
 *     name: "local.user",
 * });
 * // A user in an external identity source.
 * const external = vsphere.getSsoUser({
 *     name: "john.doe",
 *     domain: "example.com",
 * });
 * ```
 */
export function getSsoUserOutput(args: GetSsoUserOutputArgs, opts?: pulumi.InvokeOutputOptions): pulumi.Output<GetSsoUserResult> {
    opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts || {});
    return pulumi.runtime.invokeOutput("vsphere:index/getSsoUser:getSsoUser", {
        "domain": args.domain,
        "name": args.name,
    }, opts);
}

/**
 * A collection of arguments for invoking getSsoUser.
 */
export interface GetSsoUserOutputArgs {
    /**
     * The identity source domain the user belongs to. Defaults
     * to the local (system) domain.
     */
    domain?: pulumi.Input<string | undefined>;
    /**
     * The username of the user to look up.
     */
    name: pulumi.Input<string>;
}
