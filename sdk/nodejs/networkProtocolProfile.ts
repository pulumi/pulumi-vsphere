// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * The `vsphere.NetworkProtocolProfile` resource can be used to
 * create and manage network protocol profiles, also known as IP pools. A
 * network protocol profile defines a range of IP addresses, along with DNS,
 * gateway, and proxy settings, that can be associated with one or more
 * networks (standard port groups, distributed port groups, or opaque
 * networks) within a datacenter. vCenter Server uses this configuration to
 * automatically assign network settings to virtual machines during
 * provisioning and customization.
 *
 * > **NOTE:** This resource requires vCenter and is not available on direct ESX
 * connections.
 *
 * > **NOTE:** vCenter allows a network to be associated with only one
 * network protocol profile at a time. If a network in `networkIds` is already
 * associated with a different network protocol profile, vCenter will
 * silently move it away from that profile rather than rejecting the request.
 * To prevent this, the provider validates that none of the configured
 * `networkIds` are already assigned to another network protocol profile, and
 * will raise an error (both at plan time and apply time) if a conflict is
 * found.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as vsphere from "@pulumi/vsphere";
 *
 * const datacenter = vsphere.getDatacenter({
 *     name: "dc-01",
 * });
 * const network = datacenter.then(datacenter => vsphere.getNetwork({
 *     name: "VM Network",
 *     datacenterId: datacenter.id,
 * }));
 * const profile = new vsphere.NetworkProtocolProfile("profile", {
 *     name: "example-profile",
 *     datacenterId: datacenter.then(datacenter => datacenter.id),
 *     networkIds: [network.then(network => network.id)],
 *     dnsDomain: "example.com",
 *     dnsSearchPath: "example.com",
 *     ipv4: {
 *         subnet: "10.10.10.0",
 *         netmask: "255.255.255.0",
 *         gateway: "10.10.10.1",
 *         range: "10.10.10.100#100",
 *         dnsServers: [
 *             "10.10.10.10",
 *             "10.10.10.11",
 *         ],
 *     },
 * });
 * ```
 *
 * ## Import
 *
 * An existing network protocol profile can be imported into
 * this resource via a colon-separated combination of the datacenter's managed
 * object ID and either the network protocol profile's name or its numeric ID,
 * via the following command:
 *
 * [docs-import]: https://developer.hashicorp.com/terraform/cli/import
 *
 * ```sh
 * $ pulumi import vsphere:index/networkProtocolProfile:NetworkProtocolProfile profile datacenter-21:example-profile
 * ```
 *
 * or, using the numeric ID:
 *
 * ```sh
 * $ pulumi import vsphere:index/networkProtocolProfile:NetworkProtocolProfile profile datacenter-21:5
 * ```
 */
export class NetworkProtocolProfile extends pulumi.CustomResource {
    /**
     * Get an existing NetworkProtocolProfile resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: NetworkProtocolProfileState, opts?: pulumi.CustomResourceOptions): NetworkProtocolProfile {
        return new NetworkProtocolProfile(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'vsphere:index/networkProtocolProfile:NetworkProtocolProfile';

    /**
     * Returns true if the given object is an instance of NetworkProtocolProfile.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is NetworkProtocolProfile {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === NetworkProtocolProfile.__pulumiType;
    }

    /**
     * The [managed object ID][docs-about-morefs] of
     * the datacenter this network protocol profile is associated with. Forces a
     * new resource if changed.
     *
     * [docs-about-morefs]: /docs/providers/vsphere/index.html#use-of-managed-object-references-by-the-vsphere-provider
     */
    declare public readonly datacenterId: pulumi.Output<string>;
    /**
     * The DNS domain to use for this network protocol
     * profile, for example `example.com`.
     */
    declare public readonly dnsDomain: pulumi.Output<string | undefined>;
    /**
     * The DNS search path to use for this network
     * protocol profile, for example `eng.example.com;example.com`.
     */
    declare public readonly dnsSearchPath: pulumi.Output<string | undefined>;
    /**
     * The prefix to use when generating host names
     * for this network protocol profile.
     */
    declare public readonly hostPrefix: pulumi.Output<string | undefined>;
    /**
     * The HTTP proxy to use on this network, in the
     * form of a host and port, for example `proxy.example.com:3128`.
     */
    declare public readonly httpProxy: pulumi.Output<string | undefined>;
    /**
     * An IPv4 configuration block, documented below. At
     * least one of `ipv4` or `ipv6` must be specified.
     */
    declare public readonly ipv4: pulumi.Output<outputs.NetworkProtocolProfileIpv4 | undefined>;
    /**
     * An IPv6 configuration block, documented below. At
     * least one of `ipv4` or `ipv6` must be specified.
     */
    declare public readonly ipv6: pulumi.Output<outputs.NetworkProtocolProfileIpv6 | undefined>;
    /**
     * The name of the network protocol profile.
     */
    declare public readonly name: pulumi.Output<string>;
    /**
     * The managed object IDs of the networks
     * (standard port groups, distributed port groups, or opaque networks)
     * associated with this network protocol profile.
     */
    declare public readonly networkIds: pulumi.Output<string[] | undefined>;

    /**
     * Create a NetworkProtocolProfile resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: NetworkProtocolProfileArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: NetworkProtocolProfileArgs | NetworkProtocolProfileState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as NetworkProtocolProfileState | undefined;
            resourceInputs["datacenterId"] = state?.datacenterId;
            resourceInputs["dnsDomain"] = state?.dnsDomain;
            resourceInputs["dnsSearchPath"] = state?.dnsSearchPath;
            resourceInputs["hostPrefix"] = state?.hostPrefix;
            resourceInputs["httpProxy"] = state?.httpProxy;
            resourceInputs["ipv4"] = state?.ipv4;
            resourceInputs["ipv6"] = state?.ipv6;
            resourceInputs["name"] = state?.name;
            resourceInputs["networkIds"] = state?.networkIds;
        } else {
            const args = argsOrState as NetworkProtocolProfileArgs | undefined;
            if (args?.datacenterId === undefined && !opts.urn) {
                throw new Error("Missing required property 'datacenterId'");
            }
            resourceInputs["datacenterId"] = args?.datacenterId;
            resourceInputs["dnsDomain"] = args?.dnsDomain;
            resourceInputs["dnsSearchPath"] = args?.dnsSearchPath;
            resourceInputs["hostPrefix"] = args?.hostPrefix;
            resourceInputs["httpProxy"] = args?.httpProxy;
            resourceInputs["ipv4"] = args?.ipv4;
            resourceInputs["ipv6"] = args?.ipv6;
            resourceInputs["name"] = args?.name;
            resourceInputs["networkIds"] = args?.networkIds;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(NetworkProtocolProfile.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering NetworkProtocolProfile resources.
 */
export interface NetworkProtocolProfileState {
    /**
     * The [managed object ID][docs-about-morefs] of
     * the datacenter this network protocol profile is associated with. Forces a
     * new resource if changed.
     *
     * [docs-about-morefs]: /docs/providers/vsphere/index.html#use-of-managed-object-references-by-the-vsphere-provider
     */
    datacenterId?: pulumi.Input<string | undefined>;
    /**
     * The DNS domain to use for this network protocol
     * profile, for example `example.com`.
     */
    dnsDomain?: pulumi.Input<string | undefined>;
    /**
     * The DNS search path to use for this network
     * protocol profile, for example `eng.example.com;example.com`.
     */
    dnsSearchPath?: pulumi.Input<string | undefined>;
    /**
     * The prefix to use when generating host names
     * for this network protocol profile.
     */
    hostPrefix?: pulumi.Input<string | undefined>;
    /**
     * The HTTP proxy to use on this network, in the
     * form of a host and port, for example `proxy.example.com:3128`.
     */
    httpProxy?: pulumi.Input<string | undefined>;
    /**
     * An IPv4 configuration block, documented below. At
     * least one of `ipv4` or `ipv6` must be specified.
     */
    ipv4?: pulumi.Input<inputs.NetworkProtocolProfileIpv4 | undefined>;
    /**
     * An IPv6 configuration block, documented below. At
     * least one of `ipv4` or `ipv6` must be specified.
     */
    ipv6?: pulumi.Input<inputs.NetworkProtocolProfileIpv6 | undefined>;
    /**
     * The name of the network protocol profile.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The managed object IDs of the networks
     * (standard port groups, distributed port groups, or opaque networks)
     * associated with this network protocol profile.
     */
    networkIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}

/**
 * The set of arguments for constructing a NetworkProtocolProfile resource.
 */
export interface NetworkProtocolProfileArgs {
    /**
     * The [managed object ID][docs-about-morefs] of
     * the datacenter this network protocol profile is associated with. Forces a
     * new resource if changed.
     *
     * [docs-about-morefs]: /docs/providers/vsphere/index.html#use-of-managed-object-references-by-the-vsphere-provider
     */
    datacenterId: pulumi.Input<string>;
    /**
     * The DNS domain to use for this network protocol
     * profile, for example `example.com`.
     */
    dnsDomain?: pulumi.Input<string | undefined>;
    /**
     * The DNS search path to use for this network
     * protocol profile, for example `eng.example.com;example.com`.
     */
    dnsSearchPath?: pulumi.Input<string | undefined>;
    /**
     * The prefix to use when generating host names
     * for this network protocol profile.
     */
    hostPrefix?: pulumi.Input<string | undefined>;
    /**
     * The HTTP proxy to use on this network, in the
     * form of a host and port, for example `proxy.example.com:3128`.
     */
    httpProxy?: pulumi.Input<string | undefined>;
    /**
     * An IPv4 configuration block, documented below. At
     * least one of `ipv4` or `ipv6` must be specified.
     */
    ipv4?: pulumi.Input<inputs.NetworkProtocolProfileIpv4 | undefined>;
    /**
     * An IPv6 configuration block, documented below. At
     * least one of `ipv4` or `ipv6` must be specified.
     */
    ipv6?: pulumi.Input<inputs.NetworkProtocolProfileIpv6 | undefined>;
    /**
     * The name of the network protocol profile.
     */
    name?: pulumi.Input<string | undefined>;
    /**
     * The managed object IDs of the networks
     * (standard port groups, distributed port groups, or opaque networks)
     * associated with this network protocol profile.
     */
    networkIds?: pulumi.Input<pulumi.Input<string>[] | undefined>;
}
